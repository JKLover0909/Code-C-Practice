#include<bits/stdc++.h>
using namespace std;
int main(){
	int n ;
	cin >> n;
	int a[n];	
	for (int i = 0; i<n; i++){
		a[i] = i+1; cout << a[i];
	}
	for (int i = n-1; i>0; i--){
		if (a[i]>a[i-1])
		{
			thaythe = a[i];
	        for(int y = i; y<n; i++){
	        	if (a[y]>=a[i]||)
			}
		} 
	}
}
